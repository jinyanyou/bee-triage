# Anthropic API 키 (없으면 자동으로 MOCK 모드로 동작합니다)
ANTHROPIC_API_KEY=sk-ant-...

# 사용할 모델 (기본: claude-opus-5)
BEE_MODEL=claude-opus-5

# 데이터 폴더 경로 (기본: ../data/벌집플랫폼_데이터)
# BEE_DATA_DIR=C:/path/to/벌집플랫폼_데이터

# 강제로 목업 모드로 실행 (키가 있어도 API를 호출하지 않음)
# BEE_FORCE_MOCK=1
